export class Project {
  constructor (public name: string, public contact: string, public description: string, public amount: number, public purpose: string, public reward: string, public type: string, public image: string) {

  }
}
